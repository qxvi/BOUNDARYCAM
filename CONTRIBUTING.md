# Contributing

Before changing BOUNDARYCAM, preserve the boundary.

Required: keep What crossed the boundary visible, keep public-only posture, avoid trust overclaiming, update manifest/control/completion when adding surfaces, run python3 tools/validate.py, run bash tools/live-smoke.sh after deployment when changing public routes.
