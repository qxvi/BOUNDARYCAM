# Frame Status

BOUNDARYCAM uses three public statuses.

```text
BOUNDARY INCOMPLETE
CONTROLLED
CLOSED
```

`BOUNDARY INCOMPLETE` means the frame exposes missing control conditions.

`CONTROLLED` means key control conditions are present but closure is not complete.

`CLOSED` means authority, evidence, replay, recognition, recourse, and closure are represented as complete for the example frame.
